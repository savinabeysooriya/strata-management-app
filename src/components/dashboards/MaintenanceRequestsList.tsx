import { Typography } from '@mui/material';

export const MaintenanceRequestsList = () => (
  <div>
    <Typography variant="h4" gutterBottom>Maintenance RequestsList</Typography>
    {/* Add admin dashboard content */}
  </div>
);
